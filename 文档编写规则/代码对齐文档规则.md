【核心目标】

以我提供的项目代码为唯一事实依据，修正现有设计文档——消除“文档描述”与“代码实现”的偏离，确保修正后的文档：1）100% 匹配代码真实逻辑/结构/约束；2）沿用原文档框架（需调整框架时标注原因）；3）可读性、可追溯性强，方便团队维护。

【核心约束（绝对禁止+操作建议）】

1. 代码保护：绝对禁止修改任何项目代码（包括代码逻辑、字段定义、注释、格式等），你的所有操作仅针对“设计文档”的文本修正，不得对代码进行任何增删改或建议性修改；

2. Git操作建议：修正设计文档后，建议执行以下Git操作以保障文档安全性：

- 提交前：先执行`git pull`同步最新版本，避免冲突；

- 提交时：使用清晰的提交信息（如“docs: 对齐用户注册接口代码，修正参数约束与响应格式”），便于追溯；

- 风险控制：若修正范围较大，可分多次小粒度提交（如按模块/章节拆分）；提交后保留1-2个版本的文档备份，或依赖Git版本控制，若后续发现修正问题，可通过`git revert`及时回退文档至历史正确版本。

【修正规则（必须严格遵守）】

1. 优先级：代码逻辑为唯一标准——原文档与代码不一致时，完全以代码为准修正；代码有但文档缺失的内容，必须补充；文档有但代码未实现的内容，按以下方式处理：{选择：保留并标注“未落地（代码未实现）” / 直接删除}

2. 必查维度：无论项目类型，必须覆盖以下核心对比点，不遗漏关键逻辑：

- 功能逻辑：核心流程、分支判断、业务规则（如校验逻辑、默认值、状态流转）；

- 数据层面：数据结构（字段名、类型、约束、默认值、关联关系）、数据流转路径；

- 接口/交互：输入输出、调用方式、依赖关系、异常码/错误信息；

- 约束边界：权限限制、兼容范围、异常场景处理、性能阈值。

【输出要求（确保健壮可用）】

1. 输出结构（缺一不可）：

- 修正后完整文档：按原文档格式排版，保留原有章节框架，新增/修改内容与原风格统一；

- 差异清单：逐条列出「原文档内容 → 修正后内容 → 修正原因（关联代码中具体逻辑/片段）」；

- 补充说明：① 若代码中存在临时方案、待优化逻辑（需在代码中备注），需标注“现状说明”及“潜在优化方向”；② 重复Git操作建议（简要重申：建议分批次提交、清晰备注提交信息、保留回退通道）。

2. 质量标准：

- 无歧义：技术术语与代码完全一致，无模糊表述；

- 可追溯：每个修正点都能对应到具体代码片段（如“参考代码中 UserDTO 的 @Size 注解”）；

- 可复用：修正后的文档可直接替换原文档，无需二次编辑。  目前已经1修改了一部分了，你可以继续修改或者重头修改